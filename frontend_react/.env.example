# Base URL for the backend API that the React app will call.
# In development, the backend runs on http://localhost:3001 by default (see backend launchSettings).
# Example values:
#   Local:            http://localhost:3001
#   Deployed (HTTPS): https://your-backend.example.com
REACT_APP_API_BASE=http://localhost:3001
